import styled from "styled-components";

export const StyledDisplayError = styled.div`
  color: rgb(var(--primary));
  display: flex;
  align-items: center;
  background-color: rgb(var(--white));
  margin: 0 auto;
  border: 0.2rem solid rgb(var(--primary));
  margin-top: 2rem;
  padding: 1rem 2rem;

  p {
    margin-left: 0.5rem;
  }
`;
